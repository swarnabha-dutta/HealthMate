import React from 'react'

const pageHeader = ({
    icon,
    title,
    backLink = '/',
    backLabel="Back "
}) => {
  return (
    <div>pageHeader</div>
  )
}

export default pageHeader