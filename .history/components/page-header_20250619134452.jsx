import React from 'react'

const pageHeader = ({
    icon,
    title,
    backLink = '/',
    backLabel="Back to Home"
}) => {
  return (
    <div>pageHeader</div>
  )
}

export default pageHeader