import React, { useState } from 'react'

const PendingDoctors = ({doctors}) => {

  const [selectedDoctor, setSelectedDoctor] = useStatete(null);
  return (
    <div>PendingDoctors</div>
  )
}

export default PendingDoctors